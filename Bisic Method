#-----------------------------------------
# setting ONE columns of a dataframe
df.列名 = df.列名.astype(int)
# setting ALL cokumns
for i in df:
    df[i] = df[i].apply(lambda x: float(x.replace(',','')))
    
#-----------------------------------------    
# NaN replace
df.replace('\s+','', regex=True, inplace = True)  
# Drop用法
Df.drop( ): 运算逻辑为，括号里应是index或column name
